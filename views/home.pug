extends layout_nav

block content
    .home-hero
      video.hero-video(playsinline autoplay muted loop)
        source(src="/images/home-bg2.mp4" type="video/mp4")

      .hero-inner
        h1.hero-title Class happens in real time. So should your notes.
        p.hero-sub Share what you hear. See what they write. Learn together.

         
